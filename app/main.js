import React from 'react';
import ReactDOM from 'react-dom';
import Loaction from './components/Locations';

ReactDOM.render(
  <Loaction />,
  document.body.appendChild(document.createElement('div'))
);
